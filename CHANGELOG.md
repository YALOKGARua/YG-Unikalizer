## v2.3.0

### Добавлено
- Слоты: 5×3 барабана, взвешенные символы, 10 линий выплат, подсветка выигрышных клеток, анимированная прорисовка линий, быстрые пресеты ставок и управление линиями.

### Изменено
- Crash: скрыта точка краша, автокэшаут отключён, краш происходит в случайный момент во время полёта; если не успеть нажать Cashout — ставка сгорает.

### Исправлено
- Совпадение линий на слотах теперь строится по реальным центрам ячеек, SVG точно совпадает с сеткой символов на любых размерах окна.

## v2.2.0

### Добавлено
- Вкладка `Fun` с мини‑игрой Crash: плавная экспоненциальная кривая, множитель в центре, самолётик с анимированным винтом, подсветка линии и заливки.

### Улучшено
- Игровое поле сведено в центр и уменьшено: рамка, тень, соотношение 16:9 (~60vh), чтобы не занимать весь экран.
- Фон игровой области: радиальный градиент и лучи из угла, неоновое свечение кривой.

### Изменено
- Поле ставки теперь редактируемое перед стартом и после раунда, с минимальным значением 1.

### Исправлено
- Мелкие правки доступности и визуальной иерархии внутри игровой сцены.

## v2.1.0

### Улучшено
- Перцепционные хеши (a/d/p) для вкладки «Дубликаты» теперь декодируются через WIC (Windows Imaging Component), с фолбэком на sharp — меньше задержек и меньше потребление памяти.
- По умолчанию для хеша содержимого файлов используется xxHash64 (быстрее FNV1a), асинхронный воркер тоже перешёл на xxh64.
- Добавлен батч‑расчёт aHash в главном процессе (IPC `native-ahash-batch`) с ограничением конкуренции — интерфейс не блокируется при 500+ фото.
- Дедупликация по содержимому перед обработкой переведена на потоковый SHA‑256 в главном процессе.

### Исправлено
- Фризы и лаги UI при добавлении больших папок: тяжёлые операции вынесены из рендерера.

## v2.0.1

### Исправлено
- Убрана нижняя панель чата на всех вкладках; чат теперь только во вкладке «Остальное → Чат».
- Стабильный зазор под скролл (scrollbar-gutter) и стили трека — элементы справа не упираются в полосу прокрутки.
- Перенос и обтекание контролов в шапке настроек при узкой ширине (overflow‑x + flex‑wrap).

### Добавлено
- Настройка положения кнопок окна (слева/справа) в разделе «Настройки».
- Контейнерные запросы Tailwind для адаптивности гридов превью.

## v2.0.0

### Добавлено
- Полный редизайн интерфейса: разделение на «Фото и метаданные» и «Остальное».
- Новый сайдбар и современный хедер, единые стили кнопок/иконок (Iconify/Tabler).
- Командная палитра (Cmd/Ctrl+K).
- Тёмная/светлая тема, переключатель в шапке, сохранение в localStorage.
- Глубокие настройки Fake EXIF: профили (камера/телефон/дрон/экшн/сканер), ISO/выдержка/диафрагма/фокус, программа/замер/вспышка/ББ/цветовое пространство/рейтинг/label/заголовок, GPS с пресетами и ручным вводом.
- Вкладка Indigo: копирование/выделение/скачивание скрипта.
- Вкладка Vision: проверка токена/endpoint.
- Вкладка Admin: панель админа с автоподстановкой пароля (localStorage).
- Многоязычность интерфейса адаптирована под новый UI (RU/UK/EN).
- Принудительная блокировка UI при наличии обновления: модалка с прогрессом (%, MB/s, загружено/всего, ETA) и кнопкой Install.
- Кастомная верхняя панель окна (frameless) с drag‑area и собственными кнопками свернуть/развернуть/закрыть.
- Новая иконка приложения (SVG) и генерация PNG/ICO (`scripts/make-icons.js`).

### Улучшено
- Полная миграция на TypeScript (renderer + preload + main), строгая типизация IPC.
- Выделение фото‑функций в новый `NewApp.tsx`, прочие инструменты в `OtherApp.tsx`.
- Унификация иконок и размеров, улучшенные пустые состояния.
- Интервал автопроверки обновлений — 1 минута.

### Исправлено
- Стабильная подписка на события автообновления; автоматический старт загрузки при доступной версии.
- Мелкие UI‑правки и тексты i18n.

## v1.30.0

### Добавлено
- Продвинутые UI‑библиотеки: Framer Motion для анимаций, Headless UI для доступных компонентов.
- Tailwind плагины: @tailwindcss/typography для улучшенной типографики, @tailwindcss/forms для стилизованных форм.
- Улучшенные анимации модальных окон и переходов.

### Улучшено
- Полная унификация UI‑кнопок: единые `btn`, `btn-primary`, `btn-ghost` во всех панелях.
- Единый стиль чипов и счётчиков в шапке.
- Приведение действий тайлов/списков к стилям `btn-ghost`.
- Единый стиль кнопок в Chat/Auth/Dev Unlock/AdminPanel.
- Расширенная поддержка анимированных компонентов через Motion API.
- Доступные и красивые формы с автоматической стилизацией.
- Профессиональная типографика в текстовых блоках.

### Локализация
- Вынесены в i18n новые строки: копирование/выделение/скачивание, счётчики шапки, сводки конвертера, статусы Vision, заголовки/кнопки модалок.

### Разное
- Мелкие правки тёмной темы и стеклянных панелей.

## v1.29.0

### Добавлено
- Кнопка «Запустить от администратора» в шапке (скрывается, если уже под админом).
- Тост на 3 секунды «Запущено от администратора» при старте с правами администратора.
- Автоповышение прав при записи в недоступные каталоги: рендер во временный файл + копирование с UAC, если требуется.
- WebAssembly инфраструктура (AssemblyScript): сборка `public/wasm/pu.wasm`, загрузчик, COOP/COEP для SIMD/Threads.
- Иконка приложения и генерация ICO (`scripts/make-icons.js`).

### Исправлено
- Безопасная генерация имён файлов, автодобавление расширения, понятная ошибка при недоступной папке вывода.
- Контекст‑меню Проводника ставится для всех пользователей (NSIS include).

## v1.28.0

### Добавлено
- Инсталлятор (для всех пользователей): авто‑регистрация контекстного меню Проводника для изображений, папок и фона папки.
- Сохранение пользовательского состояния между версиями: язык, папка вывода, профиль/формат/качество, дрейфы, макс. ширина, схема имени, флаги метаданных, фильтры/поиск/сортировка, активная вкладка, список последних файлов.

### Улучшено
- Что нового: надёжный парсинг заметок (GitHub + локальный CHANGELOG, фолбэк к первой секции).

## v1.27.3

### Исправлено
- Автообновление: добавлен устойчивый фолбэк на GitHub Releases при сбоях провайдера.
- Прямая загрузка инсталлятора с прогрессом и запуск установки, если `quitAndInstall` недоступен.
- Заметки релиза: более надёжное получение (GitHub + CHANGELOG) для текущей/фолбэк‑версии.

### Добавлено
- Интеграция проводника: авто‑регистрация контекстного меню через NSIS (для всех пользователей).

## v1.27.2

### Добавлено
- «О программе»: отображение README как Markdown с безопасной санацией HTML.

### Исправлено
- Отрисовка Markdown‑разметки в About: заголовки, списки, ссылки, кодовые блоки.

## v1.27.1

### Исправлено
- Рендер заметок о релизе: HTML показывается корректно в модалке обновления и панели «Что нового», с безопасной санацией.

## v1.27.0

### Добавлено
- Профессиональный тёмный UI: сайдбар‑навигация, полированный хедер, стеклянные панели.
- Иконки действий и улучшенные кнопки по всему приложению.
- Мини‑полоска прогресса на каждом тайле текущего файла.
- Сводные чипы в заголовке: Total, Selected, Processed, Errors.
- Мультовыбор в гриде: Shift‑диапазон, Ctrl/Cmd добавление, прямоугольный drag‑selection.
- Контекстное меню на тайлах: Preview, Open in folder, Rename, Delete (реальные операции через IPC).
- Панель деталей справа: путь, размер, разрешение, формат, дата, хэш, быстрые действия.
- Фильтры/сортировка/поиск файлов: по расширению, имени, размеру, дате.
- Блокировка UI при доступном обновлении до установки (модальное окно с прогрессом).

### Улучшено
- Производительность списка: lazy/async для изображений, content‑visibility для тайлов.
- Глобальная панель прогресса под шапкой, aria‑live для озвучивания статусов.

### Исправлено
- Стабильный расчёт процента прогресса на основе байтов; корректный подсчёт ошибок.

## v1.26.0

### Добавлено
- Indigo: вкладка с инструкцией и встроенным отображением скрипта `indigo-script.js`, кнопки «Копировать», «Выделить всё», «Скачать .js».

### Изменено
- Чат: ник минимум 3 символа; подключение только после подтверждения ника.

### Исправлено
- Совместимость копирования Indigo‑скрипта (fallback через скрытое textarea).

## v1.25.0

### Добавлено
- Чат: возможность смены ника в интерфейсе; имя/идентификатор пользователя для каждого сообщения без подтверждения.
- Сервер чата: транслирует количество пользователей; принимает имя/идентификатор пользователя для каждого сообщения.
- Локализация: все строки чата (`tabs.chat`, `chat.*`) локализованы на русский/британский/английский языки.

### Изменено
- Конечная точка чата по умолчанию скрыта в интерфейсе; приложение по умолчанию использует IP-адрес локальной сети.

### Исправлено
- Опечатка в хуке эффекта чата; стабильное переподключение при смене URL.

## v1.24.0

### Добавлено
- Многоязычный интерфейс (селектор в заголовке). Добавлены языки: English, Русский, Українська, العربية, 中文, Polski, Español, Français, 日本語, 한국어, Қазақ тілі, Italiano, Svenska, Norsk (букмол), Suomi, Română, Română (MD).

### Изменено
- Заменены жёстко заданные строки интерфейса ключами i18n на основных панелях: «Вывод», «Профиль/Формат/Качество», «Дрейфы», «Название», «Раздел GPU», «Метаданные», «Поддельные метаданные», «Состояния прогресса», «Диалог авторизации».

### Удалено
- Интеграция устаревших версий Indigo из приложения (вкладка Indigo сохранена как «тестированная»).

### Исправлено
- Корректная обработка текста справа налево для арабского языка и постоянных языков через localStorage.

## v1.23.1

### Изменено
- Вкладка Indigo переведена в режим «Тестируется» и переключена на авторизацию по логину/паролю. Токен‑проверка из UI убрана (оставлены только Endpoint/Порт, Email и Пароль).

### Исправлено
- Небольшие правки UI и статусов.

## v1.23.0

### Добавлено
- Защита приложения паролем: опциональный запрос пароля на основе окружения/файла при запуске (поддерживает APP_PASSWORD / APP_PASSWORD_HASH, %APPDATA%\PhotoUnikalizer\app.pass и resources\app.pass). Наложение пользовательского интерфейса с функцией «Запомнить на 7 дней».

- Переработана проверка токена Indigo: ручное определение хоста и порта, автоматическое определение префикса API (/api/v2 → /api/v1), гибкая аутентификация (заголовок авторизации и резервный ?token=), расширенная проверка конечной точки.

### Изменено
- Интерфейс Indigo: конечная точка по умолчанию — http://127.0.0.1 с отдельным редактируемым полем порта.

### Исправлено
- Дублирование порта при нажатии «Проверить»: нормализация базового URL предотвращает двойной «:port».
- Избегайте ложных ошибок 404, сначала проверяя настоящие аутентифицированные конечные точки.

## v1.22.0

### Улучшено
- Адаптивный интерфейс для популярных разрешений (1024×768 → 1920×1080): сложенная компоновка на маленьких экранах, адаптивные сетки (2→3→4 столбца), панели инструментов с переносом, уменьшенная высота миниатюр/текстовых областей на мобильных устройствах, адаптивные поля баннеров.

### Добавлено
- Вкладки Indigo (скоро) и Vision (скоро) вместо «Дубли»; на текущем этапе отображаются как плейсхолдеры без функционала.

### План развития
- Импорт автиков в Indigo.
- Экспорт из Indigo в Vision.

## v1.21.2

### Улучшено
- Надёжная загрузка нативного модуля: поиск в `native/build/Release` и `native/prebuilds` (включая asar-unpacked) для работы без локальной сборки.
- Инициализация COM (`CoInitializeEx`) для стабильного WIC‑декодера.

### Исправлено
- Подписка рендерера на события автообновления и стартовая проверка — баннер обновлений снова появляется и работает скачивание/установка.

## v1.21.1

### Исправлено
- Возвращён баннер автообновления: подписка на события апдейтера в рендерере и стартовая проверка обновлений.
- Прогресс загрузки и кнопка установки обновления работают без перезапуска.

## v1.21.0

### Добавлено
- Нативный C++ парсер TXT→JSON (`parseTxtProfiles`, `parseTxtProfilesFromFile`): поддержка массивов/объектов/коллекций профилей, извлечение `profileId` из URL и cookie.
- Метрики парсинга: `errors`, `errorsInvalidJson`, `errorsUnsupported`, `segments`, `parsedSegments`.

### Улучшено
- Автовосстановление JSON: удаление висячих запятых внутри `[...]`/`{...}`.
- Нормализация и сортировка cookie: дедуп по имени (берётся последнее), важные (`c_user`, `xs`, `datr`, `sb`, `fr`) выше, стабильная сортировка по имени.
- Заголовок профиля: поддержка разделителей `|`, табуляции и `;`.
- UI конвертера: переключатель "Автоконверт", drag&drop чтение TXT/LOG/JSON, кнопки "Выделить все"/"Снять", улучшенный поиск.
- IPC/Preload: чтение текста по пути и экспорт нативного парсера для файла.

### Исправлено
- Стабильность и точность конвертации больших файлов, улучшенная дедупликация профилей и пометка важных cookie.

## v1.20.0

### Добавлено
- Шаблон Facebook: JPG, sRGB, прогрессивная развёртка, качество 85, максимальная ширина 2048, GPS очищен
- Вкладка «Дубликаты»: построение перцептивных хешей, кластеризация по Хэммингу, отображение групп
- Встроенное декодирование WIC Gray8, индекс дерева виртуальных вершин Хэмминга, API кластеризации
- Встроенное API кластеризации/поиска в предварительной загрузке: создание/запрос/освобождение индекса, clusterByHamming
- Хеш содержимого каждого файла xxHash64 и количество точек доступа SIMD для Хэмминга
- Путь декодирования Gray8 с функцией перетаскивания на основе WIC
- Вкладка интерфейса «Дубли» с предпросмотром и размерами групп

### Улучшено
- Запись метаданных: встраивание XMP/EXIF в JPG/PNG/WebP; пропуск записи при включении параметра «Удалить все метаданные»
- Принудительное использование цветового пространства sRGB при экспорте
- Учитывать максимальную ширину при применении шаблона; Изменение размера без увеличения
- Прогрессивные настройки JPEG по умолчанию для лучшей передачи
- Чёткая обработка удаления GPS без изменения других полей
- Предустановка Facebook отображается в интерфейсе и устанавливает дрейф равным нулю

### Исправлено
- Сборка на основе инструментария C++23 (проблемы с u8string), надёжная медиана в дереве виртуальных машин
- Компиляция на MSVC с исключением char8_t u8string; использование path.string()
- Стабильный прогресс IPC теперь включает outPath во всех путях кода
- Вкладка «Готово» отображает обработанные файлы одинаково

## v1.11.3

## Fixed
- Теперь фотки показываются в "Готовое"

## v1.11.2

### Added
- “О программе” в шапке: показ локального README без перехода
- Периодическая проверка обновлений каждые 10 минут (показывает баннер без рестарта)

### Improved
- Упаковка: включены `CHANGELOG.md` и `README.md` в инсталлятор
- Changelog в приложении: объединение GitHub Release Notes + локального CHANGELOG

## v1.11.1

## Add
- Readme (О программе)

## Fix
- Fixed integrated changelog

## v1.11.0

### Добавлено
- Нативное дополнение C++ (N-API): быстрое хеширование файлов, перцепционные хеширования (a/d/p), фильтрованное сканирование каталогов
- Дедупликация на основе содержимого перед обработкой
- Предварительно собранные нативные исполняемые файлы в установщике (сборка на компьютерах пользователей не производится)
- Переключение между GPU и именем адаптера в интерфейсе (в настоящее время используется резервный CPU)
- Обновление баннера со встроенными заметками о выпуске (GitHub + CHANGELOG)
- Быстрый доступ к информации о «Что нового в этой версии» в заголовке
- Ожидаемое время и скорость выполнения, системное уведомление о завершении/отмене
- Поддержка перетаскивания для конвертера TXT→JSON

### Улучшено
- Надёжная загрузка нативных модулей (предварительные сборки + распаковка ASA)
- Асинхронное хеширование и сканирование, улучшенная отчётность о ходе выполнения

### Исправлено
- Более безопасная проверка обновлений с корректными резервными копиями

## v1.10.1

Initial Electron + Приложение React, конвейер обработки, интеграция EXIF